services:
  web:
    image: ${MIRTHSPOOL_WEB_IMAGE:?set an immutable ghcr.io image digest}
    build: null

  worker:
    image: ${MIRTHSPOOL_WORKER_IMAGE:?set an immutable ghcr.io image digest}
    build: null

  migrate:
    image: ${MIRTHSPOOL_WORKER_IMAGE:?set an immutable ghcr.io image digest}
    build: null
